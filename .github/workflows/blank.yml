name: a1
on: [push]
jobs:
    run:
        name: Run
        runs-on: ubuntu-latest
        strategy:
          fail-fast: false
        steps:
          - shell: bash
            run: |
              echo "Hello from a malicious GH actions workflow!"
